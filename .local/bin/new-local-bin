#!/bin/sh

[ -z "$1" ] && echo "No filename specified." && exit 1

file="$HOME/.local/bin/$1"

if [ ! -e "$file" ]; then
	printf "#!/bin/sh\n\n\n" > "$file"
	chmod +x "$file"
fi

[ -n "$2" ] && $2 "$file"

