#!/bin/sh -e

dir=${XDG_DOCUMENTS_DIR:-"$HOME"/Documents}/notes

mkdir -p "$dir"

name="$(date "+%F")"
file="$dir/$name"

if [ -t 1 ] && [ ! -p /dev/stdout ]; then
	vim "$file"
else
	st -e vim "$file"
fi

